interface:
  display_name: "Codex Project Path Migrator"
  short_description: "Remap Codex projects while keeping chats"
  brand_color: "#2563EB"
  default_prompt: "Use $codex-project-path-migrator to update a Codex project folder path while keeping the existing chat history attached."
policy:
  allow_implicit_invocation: true
