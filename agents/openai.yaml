interface:
  display_name: "Codex Project Path Migrator"
  short_description: "Move Codex chats without losing history"
  brand_color: "#2563EB"
  default_prompt: "Use $codex-project-path-migrator to move a Codex project or global chat while preserving history and repairing restart-sensitive sidebar state."
policy:
  allow_implicit_invocation: true
